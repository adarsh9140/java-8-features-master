package com.javamultiplex.methodreference.interview.collectionQuestions;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class MapToObjeMethodExample {

    /*public static void main(String[] args) {
        List<Integer> numbers = Arrays.asList(1, 2, 3, 4, 5);

        // Square each integer and convert it to a string representation
        List<String> squaredStrings = numbers.stream()
                .mapToObj(n -> {
                    int squared = n * n; // Square the integer
                    return String.valueOf(squared); // Convert squared integer to String
                })
                .collect(Collectors.toList());

        // Print the list of squared string representations
        System.out.println("Squared string representations: " + squaredStrings);
    }*/

}
